#include <iostream>
#include <vector>
using namespace std;

int main(void)
{
    int K, N, tmp;
    vector <int>v;

    cin>>K>>N;
    for (int i = 0; i < K; i++)
    {
        cin>>tmp;
        v.push_back(tmp);
    }
}
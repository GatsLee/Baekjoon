#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int N;
int val[1001];
int max_cnt[1001];

int main(void) {

    ios_base::sync_with_stdio(0);
    cin.tie(0);


    int N;
    for(int i = 1; i <= N; i++)
        cin>>val[i];
    for (int i = 1; i <= N; i++) {
        max_cnt[i] = val[i] 
    }
}
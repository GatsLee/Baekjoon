#include <iostream>
#include <queue>
using namespace std;

int main(void)
{
    int CASE , N, M;
    int num, cnt;

    cin>>CASE;
    while (CASE--)
    {
        queue <int>q;
        cin>>N>>M;
        for (int i = 0; i < N; i++)
        {
            cin>>num;
            q.push(num);
        }

    }
}
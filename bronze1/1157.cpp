#include <iostream>
#include <algorithm>
#include <string>
using namespace std;

int main(void)
{
    size_t  idx = 0;
    int     cnt[26];
    string str;
    string  lower = "abcdefghijklmnopqrstuvwxyz";
    string  upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";

    bzero(cnt, 26);
    cin>>str;
    while (str[idx])
    {
        if (str[idx] == lower)

    }
}